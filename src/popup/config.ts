const { freeze } = Object;

/** 计时模块常量 */
export const TIMEKEEPING = freeze({
  /** 计时时长/ms */
  DURATION: 7000,
});

export const SHOW_VIDEO = freeze({
  /** 用户浏览视频数量 */
  WATCH_UPPER_LIMIT: 1, // to do
});
